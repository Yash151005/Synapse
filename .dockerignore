.git
**/.git
.gitignore
.claude

node_modules
**/node_modules
.pnpm-store

.next
**/.next
dist
apps/voice-bridge/dist
out
.turbo
coverage
*.tsbuildinfo

# .env is intentionally NOT excluded so Next.js can read NEXT_PUBLIC_* at build time.
# Keep .env.local out (local overrides) but allow the committed .env.
.env.local
**/.env.local

*.log
.bootstrap.log

.stellar
secrets.json
supabase/.branches
supabase/.temp
supabase/.env

tmp
temp
.cache
.vercel

# Mobile app not needed for Docker build
apps/mobile

# Synapse copy/zip not needed
Synapse
Synapse.zip

# Rust contracts not needed
contracts
